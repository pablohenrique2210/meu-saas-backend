# Backend NestJS
PORT=4000
API_PUBLIC_URL=https://SEU-BACKEND.up.railway.app
# No Railway, monte o volume em /data e grave os arquivos nesta subpasta.
UPLOADS_DIR=/data/uploads
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DATABASE?schema=public

# Clerk (use chaves de produção no ambiente publicado)
CLERK_SECRET_KEY=sk_live_...
CLERK_JWT_KEY="-----BEGIN PUBLIC KEY-----\n...\n-----END PUBLIC KEY-----"
CLERK_AUTHORIZED_PARTIES=https://SEU-FRONT.vercel.app

# Segredo HMAC usado para proteger CPF. Ao migrar de Railway, COPIE o mesmo
# valor do projeto antigo; trocar esta chave invalida convites pendentes.
CPF_HASH_SECRET=

# Endereços autorizados a abrir e consumir a área de RH.
# Informe exatamente dois e-mails, separados por vírgula.
RH_ALLOWED_EMAILS=pablohenrique2210@gmail.com,consultoria@lilianarruda.com.br

# Origens do frontend autorizadas a chamar a API, separadas por vírgula.
FRONTEND_URLS=https://SEU-FRONT.vercel.app,http://localhost:3000
# URL principal usada nos links enviados por e-mail (uma única origem).
FRONTEND_URL=https://SEU-FRONT.vercel.app
# Slugs dos projetos Vercel cujos domínios de Preview podem consumir a API.
VERCEL_FRONTEND_PROJECTS=pablohenrique2210-meu-saas-frontend,laconsultoria

# Armazenamento permanente para imagens, vídeos e materiais.
# Opção B: Bucket Railway/S3. O backend também reconhece automaticamente as
# variáveis nativas BUCKET, REGION, ACCESS_KEY_ID, SECRET_ACCESS_KEY e ENDPOINT.
# Buckets Railway novos usam estilo virtual; use "path" somente em buckets antigos.
S3_BUCKET=
S3_REGION=
S3_ACCESS_KEY_ID=
S3_SECRET_ACCESS_KEY=
S3_ENDPOINT=
S3_PREFIX=course-assets
S3_URL_STYLE=virtual

# Materiais de aula no Bunny Storage S3 (preferidos sobre as variáveis S3
# genéricas acima). Crie uma NOVA Storage Zone com S3 Compatibility habilitado.
# Em S3, o nome da zone também é o Access Key ID e o bucket.
BUNNY_STORAGE_ZONE_NAME=
BUNNY_STORAGE_PASSWORD=
BUNNY_STORAGE_REGION=ny
BUNNY_STORAGE_S3_ENDPOINT=https://ny-s3.storage.bunnycdn.com
BUNNY_STORAGE_PREFIX=course-materials

# Vídeos Bunny Stream: somente servidor. Mesma biblioteca configurada na Vercel.
BUNNY_LIBRARY_ID=
# Read-only API Key da biblioteca (preferida para consultas no Railway).
BUNNY_READ_ONLY_API_KEY=
# Chave de Embed View Token Authentication, obtida em Stream > Security.
BUNNY_EMBED_TOKEN_KEY=
